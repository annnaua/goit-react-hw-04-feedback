import styled from 'styled-components';

export const StatisticsList = styled.ul`
  list-style: none;

  display: flex;
  flex-direction: column;
  gap: 32px;

  padding: 0;
  margin: 0;
`;

export const StatisticsText = styled.p`
  padding: 0;
  margin: 0;

  font-size: 24px;
  font-weight: 600;
  color: #111827;
`;

export const StatisticsVolume = styled.span`
  padding: 0;
  margin: 0;

  font-size: 24px;
  font-weight: 600;
  color: #111827;
`;
